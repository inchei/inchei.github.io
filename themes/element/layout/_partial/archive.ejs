<div class="page-header">
	<% if (is_tag()) { %>
		- <%= page.tag %> -
	<% } %>

</div>

<ul class="post-list clearfix">
  <% page.posts.each(function(post){ %>
    <%- partial('post', {post: post}) %>
  <% }) %>
</ul>

<% if (page.total > 1){ %>
  <nav id="page-nav">
    <%- paginator({
      prev_text: '新',
      next_text: '舊'
    }) %>
  </nav>
<% } %>

